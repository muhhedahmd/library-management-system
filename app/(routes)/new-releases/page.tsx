import type { Metadata } from "next"

export const metadata: Metadata = {
  title: "New Releases",
  description: "Discover the latest book additions to our library collection.",
}

const page = () => {
  return (
    <div>page</div>
  )
}

export default page
