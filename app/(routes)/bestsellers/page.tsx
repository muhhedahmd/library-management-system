import type { Metadata } from "next"

export const metadata: Metadata = {
  title: "Bestsellers",
  description: "Browse the top bestselling books in our collection.",
}

const page = () => {
  return (
    <div>page</div>
  )
}

export default page
